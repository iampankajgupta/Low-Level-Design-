package LLD.Elevator.Strategy;

import LLD.Elevator.model.Elevator;

public class EvenMoving implements ElevatorMovingStrategy{
    @Override
    public void move(Elevator elevator) {
        // check for internal requqest is empty then starting moving to ground floor

    }
}
